apiVersion: 1

# Auto-connect Grafana to the Prometheus service (everything in Docker).
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
